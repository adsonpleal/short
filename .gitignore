# Compiled binaries
/short
/short-linux
/export
*.exe

# Link database (local + never commit server data)
*.db

# Deploy skill contains server details (IP, SSH key path) — kept local on purpose
.claude/skills/deploy/
.claude/settings.local.json

# OS junk
Thumbs.db
.DS_Store

# Cloudflare Worker
node_modules/
.wrangler/
.dev.vars
worker-configuration.d.ts

# Migration working directory: bbolt snapshots, exports, D1 backups, transcripts.
# Contains the only offline copies of the link data — keep the files, not in git.
/migration/
